<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Urban Solz</title>
<style>
/* === RESET & BASE === */
*{margin:0;padding:0;box-sizing:border-box}
body{
  font-family:'Poppins',sans-serif;
  background:linear-gradient(180deg,#f6f0e9 0%,#f5deb3 100%);
  color:#3e2723;
  overflow-x:hidden;
  scroll-behavior:smooth;
}
a{text-decoration:none;color:inherit}

/* === HEADER === */
header{
  background:rgba(255,255,255,0.95);
  backdrop-filter:blur(10px);
  position:sticky;top:0;z-index:1000;
  display:flex;justify-content:space-between;align-items:center;
  padding:0.8rem 6%;
  box-shadow:0 4px 10px rgba(0,0,0,0.05);
}
header h1{font-size:1.6rem;font-weight:700;letter-spacing:1px;text-transform:uppercase;color:#3e2723;}
nav a{margin-left:25px;font-weight:500;color:#4e342e;transition:color .3s}
nav a:hover{color:#a1887f}

/* === HERO === */
.hero{
  position:relative;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;
  padding:100px 8%;overflow:hidden;
  background:linear-gradient(135deg,#fff8ef,#f5deb3);
}
.hero::before{
  content:"";position:absolute;top:0;left:0;width:100%;height:100%;
  background:url('https://images.unsplash.com/photo-1606813902917-0e6767bbf4e2?auto=format&fit=crop&w=1600&q=80') center/cover no-repeat;
  opacity:0.25;z-index:-1;animation:bgmove 18s ease-in-out infinite alternate;
}
@keyframes bgmove{from{transform:scale(1);}to{transform:scale(1.08);}}
.hero-text{color:#3e2723;max-width:550px;animation:fadeslide 1s ease forwards;}
.hero-text h2{font-size:3rem;font-weight:800;text-transform:uppercase;margin-bottom:15px;letter-spacing:2px;}
.hero-text p{font-size:1.1rem;margin-bottom:25px;line-height:1.6;color:#4e342e;}
.hero-text button{
  background:#3e2723;color:#fff;border:none;border-radius:30px;
  padding:12px 40px;font-size:1rem;font-weight:600;cursor:pointer;
  transition:transform .3s,background .3s,color .3s;
  box-shadow:0 5px 15px rgba(0,0,0,0.15);
}
.hero-text button:hover{background:#a1887f;color:#3e2723;transform:scale(1.07);}
.hero img{width:420px;max-width:100%;border-radius:25px;animation:float 4s ease-in-out infinite;box-shadow:0 15px 30px rgba(0,0,0,0.2);}
@keyframes fadeslide{from{opacity:0;transform:translateY(40px);}to{opacity:1;transform:translateY(0);}}
@keyframes float{0%,100%{transform:translateY(0);}50%{transform:translateY(-15px);}}

/* === PRODUCT GRID === */
.product-grid{
  display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:40px;padding:80px 8%;
}
.product-card{
  background:#fff;border-radius:25px;
  box-shadow:0 10px 30px rgba(0,0,0,0.08);
  overflow:hidden;opacity:0;transform:translateY(60px);
  transition:all .8s ease;
}
.product-card.visible{opacity:1;transform:translateY(0);}
.product-card:hover{transform:translateY(-10px);}
.product-card img{width:100%;height:240px;object-fit:cover;transition:transform .4s;}
.product-card:hover img{transform:scale(1.08);}
.product-info{text-align:center;padding:22px;color:#3e2723;}
.product-info h3{font-size:1.2rem;margin-bottom:8px;}
.product-info p{font-size:.95rem;color:#4e342e;margin-bottom:10px;}
.product-info strong{display:block;margin-bottom:15px;font-size:1.1rem;}
.product-card button{
  background:#3e2723;color:#fff;border:none;border-radius:25px;
  padding:10px 25px;cursor:pointer;font-size:.95rem;transition:all .3s;
}
.product-card button:hover{background:#a1887f;color:#3e2723;transform:scale(1.05);}

/* === TRENDING SLIDER === */
.trending-section{
  padding:60px 0;background:linear-gradient(135deg,#fff8ef,#f5deb3);
  text-align:center;overflow:hidden;
}
.trending-section h2{
  font-size:2rem;font-weight:700;margin-bottom:25px;color:#3e2723;
  text-transform:uppercase;letter-spacing:1px;
}
.trending-slider{
  display:flex;gap:30px;
  width:max-content;
  animation:scroll 40s linear infinite;
}
.trending-slider:hover{animation-play-state:paused;}
.trending-item{
  background:#fff;border-radius:20px;box-shadow:0 8px 20px rgba(0,0,0,0.1);
  padding:15px;width:240px;flex:0 0 auto;transition:transform .3s;
  overflow:hidden;
}
.trending-item img,
.trending-item .placeholder{
  width:100%;height:200px;border-radius:15px;
  background:#ddd;position:relative;overflow:hidden;
}
.trending-item p{margin-top:10px;font-weight:500;color:#4e342e;}
.trending-item:hover{transform:scale(1.05);}

/* Shimmer effect for missing/placeholder images */
.placeholder::after{
  content:"";
  position:absolute;
  top:0;left:-150px;
  width:100px;height:100%;
  background:linear-gradient(90deg,rgba(255,255,255,0),rgba(255,255,255,0.5),rgba(255,255,255,0));
  animation:shimmer 1.5s infinite;
}
@keyframes shimmer{
  0%{left:-150px;}
  100%{left:100%;}
}

/* Infinite scroll effect */
@keyframes scroll{
  0%{transform:translateX(0);}
  100%{transform:translateX(-50%);}
}

/* === FOOTER === */
footer{
  background:#2e1f12;color:#e9d5b4;
  padding:60px 8% 30px;
}
.footer-grid{
  display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:40px;
  margin-bottom:40px;
}
.footer-grid h4{color:#fff;margin-bottom:15px;font-size:1.1rem;font-weight:600;}
.footer-grid a,.footer-grid p{
  display:block;font-size:.9rem;color:#d7c4a3;margin-bottom:10px;
  transition:color .3s;
}
.footer-grid a:hover{color:#fff;}
.footer-bottom{
  border-top:1px solid rgba(255,255,255,0.1);
  padding-top:20px;text-align:center;font-size:.85rem;color:#cdb48a;
}
.socials a{margin:0 10px;font-size:1.2rem;color:#d7c4a3;transition:color .3s,transform .3s;}
.socials a:hover{color:#fff;transform:translateY(-3px);}

/* === RESPONSIVE === */
@media(max-width:900px){
  .hero{flex-direction:column;text-align:center;padding:80px 6%;}
  .hero-text h2{font-size:2.5rem;}
  .hero img{margin-top:40px;width:80%;}
}
@media(max-width:600px){
  header h1{font-size:1.3rem;}
  .hero-text h2{font-size:2rem;}
  .hero-text p{font-size:1rem;}
  footer{padding:40px 6% 25px;}
}
</style>
</head>
<body>
<header>
  <h1>Urban Solz</h1>
  <nav>
    <a href="/cart">Cart (<%= (cart || []).reduce((s,i)=>s+i.quantity,0) %>)</a>
    <a href="/admin/login">Admin</a>
  </nav>
</header>

<section class="hero">
  <div class="hero-text">
    <h2>Runway Ready</h2>
    <p>Urban Solz — where streetwear meets luxury. Discover fashion-forward footwear crafted to turn sidewalks into runways.</p>
    <button onclick="window.location.href='/cart'">Shop the Collection</button>
  </div>
  <img src="https://images.unsplash.com/photo-1600185365483-26d7a4cc7519?auto=format&fit=crop&w=900&q=80" alt="Urban Solz Shoe">
</section>

<main>
  <div class="product-grid">
    <% products.forEach(p => { %>
      <div class="product-card">
        <img src="<%= p.image %>" alt="<%= p.name %>">
        <div class="product-info">
          <h3><%= p.name %></h3>
          <p><%= p.description %></p>
          <strong>$<%= p.price %></strong>
          <form action="/add-to-cart/<%= p._id %>" method="POST">
            <button>Add to Cart</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</main>

<!-- === TRENDING SECTION === -->
<section class="trending-section">
  <h2>Trending Now</h2>
  <div class="trending-slider">
    <% for(let i=0;i<8;i++){ %>
    <div class="trending-item">
      <div class="placeholder"></div>
      <p>Coming Soon...</p>
    </div>
    <% } %>
  </div>
</section>

<footer>
  <div class="footer-grid">
    <div>
      <h4>Urban Solz</h4>
      <p>Luxury footwear redefining urban fashion.<br> Crafted with precision, designed for confidence.</p>
    </div>
    <div>
      <h4>Shop</h4>
      <a href="#">Men</a>
      <a href="#">Women</a>
      <a href="#">New Arrivals</a>
      <a href="#">Collections</a>
    </div>
    <div>
      <h4>Support</h4>
      <a href="#">Contact Us</a>
      <a href="#">Shipping & Returns</a>
      <a href="#">FAQ</a>
      <a href="#">Privacy Policy</a>
    </div>
    <div>
      <h4>Connect</h4>
      <div class="socials">
        <a href="https://instagram.com" target="_blank">📸</a>
        <a href="https://facebook.com" target="_blank">📘</a>
        <a href="https://twitter.com" target="_blank">🐦</a>
        <a href="https://tiktok.com" target="_blank">🎵</a>
      </div>
      <p>Email: support@urbansolz.com<br>Tel: +1 (800) 555-SOLZ</p>
    </div>
  </div>
  <div class="footer-bottom">
    &copy; <%= new Date().getFullYear() %> Urban Solz. All Rights Reserved.
  </div>
</footer>

<script>
const cards=document.querySelectorAll('.product-card');
function revealOnScroll(){
  const trigger=window.innerHeight*0.9;
  cards.forEach(c=>{
    if(c.getBoundingClientRect().top<trigger)c.classList.add('visible');
  });
}
window.addEventListener('scroll',revealOnScroll);
revealOnScroll();
</script>
</body>
</html>
